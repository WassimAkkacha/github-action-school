name: workflows-steps-jobs
on: workflow_dispatch
jobs:
    echo-hello:
        runs-on: ubuntu-latest
        steps:
            - name: Say hello
              run: echo "Hello, world!"
    echo-goodbye:
        runs-on: ubuntu-latest
        steps:
            - name: Say goodbye
              run: echo "goodbye!"
